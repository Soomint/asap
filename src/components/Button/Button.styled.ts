import styled from 'styled-components';
import { btnMid } from '../../styles/mixins';

export const StyledButton = styled.button`
    ${btnMid}
    background-color: powderblue;
    color: #ffffff;
    border: none;
    border-radius: 5px;
    padding: 12px 24px;
    margin: 0;
`;